package domain

//GlobalDIParameters is a struct for storing global parameters which we want to pass through DI
type GlobalDIParameters struct {
	//RootPath is a path to project root
	RootPath string
}
